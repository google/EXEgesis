# Kernel from Bob Jenkins's mix() with seed (numeric constants redacted).
# The author claims "18 cycles/iteration if you're lucky".
add    ebx, 0x87654321
xor    ebx, 0x4567
mov    eax, 0x12345678
sub    eax, ebx
mov    ecx, ebx
shl    ecx, 0x8
xor    ecx, eax
mov    eax, 0x23456789
sub    eax, ebx
sub    eax, ecx
mov    edx, ecx
shr    edx, 0xd
xor    edx, eax
sub    ebx, ecx
sub    ebx, edx
mov    eax, edx
shr    eax, 0xc
xor    eax, ebx
sub    ecx, edx
sub    ecx, eax
mov    edi, eax
shl    edi, 0x11
xor    edi, ecx
sub    edx, eax
sub    edx, edi
mov    ecx, edi
shr    ecx, 0x6
xor    ecx, edx
sub    eax, edi
sub    eax, ecx
mov    edx, ecx
shr    edx, 0x5
xor    edx, eax
sub    edi, ecx
sub    edi, edx
sub    ecx, edx
shl    edx, 0x9
xor    edx, edi
sub    ecx, edx
shr    edx, 0x8
xor    edx, ecx
